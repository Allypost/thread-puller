<img src="<%= ResourceWatcher.getFile('static/cog.png').uri %>" alt="Settings" id="settings" title="Settings" />
<div id="settings-modal">
    <div class="settings-modal-content"></div>
</div>
<script>
    if (window.Settings) {
        var settings = new Settings();
        window.__settingsListeners = settings.listenToHandlerRequests(window.__settingsListeners);
    }
</script>
